# ===================================
# Scrappers - Variables de Entorno
# Copia este archivo a .env y configura los valores
# ===================================

# === Supabase (REQUERIDO) ===
# URL de tu proyecto Supabase
SUPABASE_URL=https://your-project.supabase.co

# Service Role Key (NO la anon key)
# Encontrar en: Supabase Dashboard > Settings > API > service_role key
# ⚠️  Esta key bypassa RLS - mantener segura
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# === Aplicación ===
APP_ENV=development  # development | staging | production
DEBUG=true           # true en desarrollo, false en producción
LOG_LEVEL=INFO       # DEBUG | INFO | WARNING | ERROR

# === API ===
API_HOST=0.0.0.0
API_PORT=8000

# === Scraping ===
SCRAPING_TIMEOUT=30          # Timeout en segundos para requests
SCRAPING_RETRY_ATTEMPTS=3    # Intentos de reintento
SCRAPING_BATCH_SIZE=100      # Productos por lote para DB insert

# === Railway (se inyectan automáticamente en producción) ===
# PORT=8000  # Railway inyecta esto
# RAILWAY_ENVIRONMENT=production
